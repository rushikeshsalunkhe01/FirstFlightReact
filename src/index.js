import { createRoot } from "react-dom/client";

// Find the root element
const rootElement = document.getElementById("root");

// Create a React root
const root = createRoot(rootElement);

// Render content
root.render(<h1>First Flight... Bhurrrrrr🚀❤</h1>);